@inject NavigationManager NavigationManager

@code {
    [Parameter]
    public string To { get; set; } = "/";
    [Parameter]
    public bool ForceLoad { get; set; } = false;

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(To, ForceLoad);
    }
}
